# latte-code

Latte 是 Claude Code 的一个可构建分支，去除了遥测功能并解锁了实验特性。支持中文界面和多模型接入。

## 安装

```bash
npm install -g latte-code
```

安装后，命令行工具名为 `latte`：

```bash
# 交互模式
latte

# 单次查询
latte -p "列出当前目录的文件"
```

更多文档请参考：https://github.com/wxj-1019/latte-code
